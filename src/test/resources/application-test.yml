spring:
  datasource:
    username: postgres
    password: testpassword

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate

  rabbitmq:
    listener:
      simple:
        retry:
          enabled: true
          max-attempts: 2
          initial-interval: 10000
          multiplier: 1


jwt:
  secret: secrethere
  accessExpiration: 10
  refreshExpiration: 60


resend:
  api:
    key: re_key